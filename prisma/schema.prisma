datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model bdhktsnu9wdebdurq47e {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  phone     String
  course    String
  createdAt DateTime @default(now())
}
